def findMaxNum(arr,n):
  arr.sort(reverse=true)
  num=arr[0]
    for i in range(1,n):
      num=num*10+arr[i]
      return num
      if_name_=="_main_"
      arr[5,6,7,8,9]
      n=len(arr)
      print(findMaxNum(arr,n))
